services:
  - type: web
    name: pntt-truyen
    env: node
    buildCommand: ""
    startCommand: node server.js
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 20
    autoDeploy: true
